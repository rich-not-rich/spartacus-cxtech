import { DefaultLayoutConfig } from './default-layout-config';

describe('DefaultLayoutConfig', () => {
  it('should create an instance', () => {
    expect(new DefaultLayoutConfig()).toBeTruthy();
  });
});
